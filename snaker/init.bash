#!/usr/bin/env bash
# The Main File of Snaker. It loads every needed module.

# shellcheck disable=SC1091

source "/usr/lib/snaker/env.bash";
source "$SNAKER_PATH/loader.bash";
source "$SNAKER_PATH/functions.bash";
